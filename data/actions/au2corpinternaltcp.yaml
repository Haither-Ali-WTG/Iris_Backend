---
destination_port_test:
  description: Test destination port to determine which jump host to forward to
  order: 50
  actions: 
    - use_backend admin-au2_wtg_zone          if { dst_port 1061 }
    - use_backend jump2-au2_wtg_zone          if { dst_port 1062 }
    - use_backend jump2-au2_wisecloud_zone    if { dst_port 1066 }
    - use_backend admin-au2_wisecloud_zone    if { dst_port 1075 }
