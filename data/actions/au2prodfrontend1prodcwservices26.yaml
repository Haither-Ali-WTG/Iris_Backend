---
secure_cookie:
  description: |-
    Set secure attribute
  order: 60
  actions:
    - http-response replace-header Set-Cookie (.*) "\1; Secure" if https !secured_cookie

https_redirect:
  description: |-
    Redirect HTTP to HTTPS (worth redirecting http to https for the domains
    covered by wildcard certificates)
  order: 79
  actions:
    - redirect scheme https if star_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_cfs_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_webcampaign_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_webprint_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_webtracker_wisegrid_net !{ ssl_fc }

cw1_use_dynamic_backend_path:
  description: |-
    Redirect Glow webservice requests to the correct backend based on path
  order: 80
  actions:
    - >-
        use_backend %[req.hdr(Host),field(1,:),lower,regsub(\.,_,g)]-glow
        if glow_webservice_path www-star_wisegrid_net

custom_url_actions:
  description: |-
    Customurls for GLOW & Webtracker
  order: 80
  actions:
    ### CFS
    - use_backend citmel_cfs_wisegrid_net if { hdr(host),field(1,:) -i cfs.citotransport.com.au }
    - use_backend nlpmel_cfs_wisegrid_net if { hdr(host),field(1,:) -i cfs.navia.com.au }
    - use_backend nlpmel_cfs_wisegrid_net if { hdr(host),field(1,:) -i nlpmel2.cfs.wisegrid.net }
    - use_backend outmel_cfs_wisegrid_net if { hdr(host),field(1,:) -i unpacks.thecargowarehouse.com.au }
    - use_backend sumsyd_cfs_wisegrid_net if { hdr(host),field(1,:) -i cfs.sgl.com.au }
    ### Webtracker
    - use_backend ahwprd-cn_webtracker_wisegrid_net if { hdr(host),field(1,:) -i ahwprd.webtracker.carozplus.com }
    - use_backend ap3syd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.allports-syd.com.au }
    - use_backend acamel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.acaint.com.au }
    - use_backend asamel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i asamel2.webtracker.wisegrid.net }
    - use_backend asamel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtrack.axima.com.au }
    - use_backend asamel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtrack.natrio.com }
    - use_backend asamel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.yangkee.com }
    - use_backend awhadl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i ulink.awh.com.au }
    - use_backend c0nall_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.consolalliance.com.au }
    - use_backend ccpbne_webtracker_wisegrid_net if { hdr(host),field(1,:) -i customers.commercialfreight.com.au }
    - use_backend citmel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.citotransport.com.au }
    - use_backend dtksgn_webtracker_wisegrid_net if { hdr(host),field(1,:) -i e-business.tmforwarding.com }
    - use_backend eplsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.eplint.com }
    - use_backend excsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.excellogistics.com.au }
    - use_backend famsin_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.famous.com.sg }
    - use_backend famsin_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.fsmac.com }
    - use_backend fragbl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.fracht.com.au }
    - use_backend fragbl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.mbowens.com.au }
    - use_backend giasyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtrack.gava.com.au }
    - use_backend gvlhst_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.gvi.co.nz }
    - use_backend hglsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.pch.com.au }
    - use_backend hi0prd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i missioncontrol.hiltons.co.nz }
    - use_backend icesyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.icecargo.com.au }
    - use_backend jjlsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.jjlawson.com.au }
    - use_backend kwesyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.kwe.com.au }
    - use_backend leaakl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i track.oceania.kln.com }
    - use_backend mlasyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.nnraus.com.au }
    - use_backend mtasyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.mantonairsea.com.au }
    - use_backend nlpmel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.navia.com.au }
    - use_backend ospakl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i ediweb.oceanbridge.co.nz }
    - use_backend pcfsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.pcfs.com.au }
    - use_backend powsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.powerhousensw.com.au }
    - use_backend silbne_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracking.sila.net.au }
    - use_backend sumsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.sgl.com.au }
    - use_backend tmssyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.totalmaritime.com.au }
    ### GLOW
    - use_backend www-famsin_wisegrid_net if { hdr(host),field(1,:) -i neo.famous.com.sg }
    - use_backend www-jjlsyd_wisegrid_net if { hdr(host),field(1,:) -i svc.jjlawson.com.au }
