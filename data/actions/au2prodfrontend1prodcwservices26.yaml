---
secure_cookie:
  description: |-
    Set secure attribute
  order: 60
  actions:
    - http-response replace-header Set-Cookie (.*) "\1; Secure" if https !secured_cookie

https_redirect:
  description: |-
    Redirect HTTP to HTTPS (worth redirecting http to https for the domains
    covered by wildcard certificates)
  order: 79
  actions:
    - redirect scheme https if star_wisegrid_net !{ ssl_fc } !excluded_from_redirect_path
    - redirect scheme https if star_cfs_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_webcampaign_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_webprint_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_webtracker_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_pda_wisegrid_net !{ ssl_fc }
    - redirect scheme https if star_spweb_wisegrid_net !{ ssl_fc }

cw1_use_dynamic_backend_path:
  description: |-
    Redirect Glow webservice requests to the correct backend based on path
  order: 80
  actions:
    - >-
        use_backend %[req.hdr(Host),field(1,:),lower,regsub(\.,_,g)]-glow
        if glow_webservice_path www-star_wisegrid_net

custom_url_actions:
  description: |-
    Customurls for CW Webservices
  order: 80
  actions:
    ### CFS
    - use_backend cfaprd_cfs_wisegrid_net if { hdr(host),field(1,:) -i cfs.cfalogistics.com.au }
    - use_backend citmel_cfs_wisegrid_net if { hdr(host),field(1,:) -i cfs.citotransport.com.au }
    - use_backend fragbl_cfs_wisegrid_net if { hdr(host),field(1,:) -i cfs.fracht.com.au }
    - use_backend nlpmel_cfs_wisegrid_net if { hdr(host),field(1,:) -i cfs.navia.com.au }
    - use_backend nlpmel_cfs_wisegrid_net if { hdr(host),field(1,:) -i nlpmel2.cfs.wisegrid.net }
    - use_backend outmel_cfs_wisegrid_net if { hdr(host),field(1,:) -i unpacks.thecargowarehouse.com.au }
    - use_backend sumsyd_cfs_wisegrid_net if { hdr(host),field(1,:) -i cfs.sgl.com.au }

    ### Webtracker
    - use_backend abqprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i abqprdwebtracker.wisegrid.net }
    - use_backend acamel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.acaint.com.au }
    - use_backend ahwprd-cn_webtracker_wisegrid_net if { hdr(host),field(1,:) -i ahwprd.webtracker.carozplus.com }
    - use_backend ahwprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i ahwprd-cn.webtracker.wisegrid.net }
    - use_backend anfprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i anfprd-cn.webtracker.wisegrid.net }
    - use_backend ap3syd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.allports-syd.com.au }
    - use_backend apomel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i apomelglow.wisegrid.net }
    - use_backend aqpprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i aqpprdwebtracker.wisegrid.net }
    - use_backend astmnl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i trackntrace.asti.com.ph }
    - use_backend awhadl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i ulink.awh.com.au }
    - use_backend awhadl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i awhadlwebtracker.wisegrid.net }
    - use_backend awhadl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i awhadlwebtracker2.wisegrid.net }
    - use_backend btimel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i btimelwebtracker.wisegrid.net }
    - use_backend bwamel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i bwamelwebtracker.wisegrid.net }
    - use_backend c0nall_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.consolalliance.com.au }
    - use_backend ccpbne_webtracker_wisegrid_net if { hdr(host),field(1,:) -i customers.commercialfreight.com.au }
    - use_backend cfaprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.cfalogistics.com.au }
    - use_backend cogsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i cogsydwebtracker.wisegrid.net }
    - use_backend citmel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.citotransport.com.au }
    - use_backend dtksgn_webtracker_wisegrid_net if { hdr(host),field(1,:) -i e-business.tmforwarding.com }
    - use_backend eplsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.eplint.com }
    - use_backend eplsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i eplsyd-webtracker.wisegrid.net }
    - use_backend excsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.excellogistics.com.au }
    - use_backend exkprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i exkprdwebtracker.wisegrid.net }
    - use_backend exkprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i exkprd-webtracker.wisegrid.nett }
    - use_backend famsin_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.fsmac.com }
    - use_backend famsin_webtracker_wisegrid_net if { hdr(host),field(1,:) -i famsin2webtracker.wisegrid.net }
    - use_backend fragbl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.fracht.com.au }
    - use_backend giasyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtrack.gava.com.au }
    - use_backend gvlhst_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.gvi.co.nz }
    - use_backend h43prd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i h43prdwebtracker.wisegrid.net }
    - use_backend hglsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.pch.com.au }
    - use_backend hi0prd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i missioncontrol.hiltons.co.nz }
    - use_backend icesyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.icecargo.com.au }
    - use_backend ifcmel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i ifcmel-webtracker.wisegrid.net }
    - use_backend jjlsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.jjlawson.com.au }
    - use_backend kwesyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.kwe.com.au }
    - use_backend leaakl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i track.oceania.kln.com }
    - use_backend lmiprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i lmiprd2.webtracker.wisegrid.net }
    - use_backend mlasyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.nnraus.com.au }
    - use_backend mtasyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.mantonairsea.com.au }
    - use_backend nlpmel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.navia.com.au }
    - use_backend oglbne_webtracker_wisegrid_net if { hdr(host),field(1,:) -i oglbne-webtracker.wisegrid.net }
    - use_backend ospakl_webtracker_wisegrid_net if { hdr(host),field(1,:) -i ediweb.oceanbridge.co.nz }
    - use_backend pcfsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.pcfs.com.au }
    - use_backend powsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.powerhousensw.com.au }
    - use_backend slxprd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i slxprdwebtracker.wisegrid.net }
    - use_backend silbne_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracking.sila.net.au }
    - use_backend sipsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i sipsydwebtracker.wisegrid.net }
    - use_backend sumsyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i webtracker.sgl.com.au }
    - use_backend tmssyd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tracking.totalmaritime.com.au }
    - use_backend tplmel_webtracker_wisegrid_net if { hdr(host),field(1,:) -i tplmelwebtracker.wisegrid.net }
    - use_backend u18prd_webtracker_wisegrid_net if { hdr(host),field(1,:) -i u18prdwebtracker.wisegrid.net }

    ### GLOW
    - use_backend www-jjlsyd_wisegrid_net if { hdr(host),field(1,:) -i svc.jjlawson.com.au }
    - use_backend www-fragbl_wisegrid_net if { hdr(host),field(1,:) -i tracking.mbowens.com.au }

    ### Accounting
    - use_backend anfprdaccounting_wisegrid_net if { hdr(host),field(1,:) -i anfprdaccounting-cn.wisegrid.net }
    - use_backend cy0sydaccounting_wisegrid_net if { hdr(host),field(1,:) -i cy0sydaccounting-cn.wisegrid.net }
    - use_backend exkprdaccounting_wisegrid_net if { hdr(host),field(1,:) -i exkprdaccounting-cn.wisegrid.net }
    - use_backend h43prdaccounting_wisegrid_net if { hdr(host),field(1,:) -i h43prdaccounting-cn.wisegrid.net }
    - use_backend oc6hkgaccounting_wisegrid_net if { hdr(host),field(1,:) -i oc6hkgaccounting-cn.wisegrid.net }
    - use_backend sjiprdaccounting_wisegrid_net if { hdr(host),field(1,:) -i sjiprdaccounting-cn.wisegrid.net }

    ### WebPrint
    - use_backend acamel_webprint_wisegrid_net if { hdr(host),field(1,:) -i acamel-cn.webprint.wisegrid.net }
    - use_backend ahwprd_webprint_wisegrid_net if { hdr(host),field(1,:) -i ahwprd-cn.webprint.wisegrid.net }
    - use_backend anfprd_webprint_wisegrid_net if { hdr(host),field(1,:) -i anfprd-cn.webprint.wisegrid.net }
    - use_backend h43prd_webprint_wisegrid_net if { hdr(host),field(1,:) -i h43prd-cn.webprint.wisegrid.net }
    - use_backend lmiprd_webprint_wisegrid_net if { hdr(host),field(1,:) -i lmiprd-cn.webprint.wisegrid.net }
    - use_backend nzpakl_webprint_wisegrid_net if { hdr(host),field(1,:) -i nzpaklwebprint.wisegrid.net }
    - use_backend srkhkg_webprint_wisegrid_net if { hdr(host),field(1,:) -i srkhkg-cn.webprint.wisegrid.net }
    - use_backend us5prd_webprint_wisegrid_net if { hdr(host),field(1,:) -i us5prd-cn.webprint.wisegrid.net }

    ### eAdaptor
    - use_backend anfprdservices_wisegrid_net if { hdr(host),field(1,:) -i anfprdservices-cn.wisegrid.net }
    - use_backend slmprdservices_wisegrid_net if { hdr(host),field(1,:) -i slmprdservices-cn.wisegrid.net }

    ### Edirf
    - use_backend ax4prd_edirf_wisegrid_net if { hdr(host),field(1,:) -i ax4prd-cn.edirf.wisegrid.net }
