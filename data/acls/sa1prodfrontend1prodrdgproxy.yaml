---
rdg_rpc_path:
  description: RPC rdg path /rpc
  tests:
    - path_beg -i /rpc

rdg_token_host:
  description: hostnames used by token auth CORP rdg endpoints (ediprod)
  tests:
    - hdr(host),field(1,:) -i rd-sa1wp-tkn-4-1.wisegrid.net
    - hdr(host),field(1,:) -i sacloud-sa1-token.wisetechglobal.cn
