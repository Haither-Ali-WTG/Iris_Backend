---
star_wisegrid_net:
  description: "*.wisegrid.net"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?wisegrid\.net$

star_borderwise_com:
  description: "*.borderwise.com"
  tests:
    - hdr(host),field(1,:) -m reg -i ^([^\.]+\.)?borderwise\.com$

au2s3-3_wtg_ws_and_subdomains:
  description: "*.au2s3-3.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^(.*)?au2s3-3\.wtg\.ws$

de1s3-3_wtg_ws_and_subdomains:
  description: "*.de1s3-3.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^(.*)?de1s3-3\.wtg\.ws$

us2s3-3_wtg_ws_and_subdomains:
  description: "*.us2s3-3.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^(.*)?us2s3-3\.wtg\.ws$

au2s3-3-primary_not_enough_capacity:
  description: "au2s3-3.wtg.ws Primary backends unavailable"
  tests:
    - nbsrv(au2s3-3-primary) lt 1

star_cwconnector_wisegrid_net:
  description: "*.cwconnector.wisegrid.net cwconnector production services"
  tests:
    - hdr(host),field(1,:) -m reg -i ^([^\.]+\.)?cwconnector\.wisegrid\.net$

star_wisetechacademy_com:
  description: "Staging Wisetechacademy"
  tests:
    - hdr(host),field(1,:),lower -m reg -i ^([^\.]+\.)?wisetechacademy\.com$

lms_staging_wisetechacademy_com:
  description: "LMS Staging Wisetechacademy"
  tests:
    - hdr(host),field(1,:) -i lms.staging.wisetechacademy.com

custom_internal_au2prod1_ocp_wtg_ws:
  description: "Custom internal au2prod1 OCP WTG WS hosts"
  tests:
    - hdr(host) -i proget-au.wtg.zone
    - hdr(host) -i proget-au.wisetechglobal.com
    - hdr(host) -i cwbot-corp.wtg.zone
    - hdr(host) -i devtools-experiments.wtg.zone
    - hdr(host) -i devtools-experiments-staging.wtg.zone
    - hdr(host) -i dgsegregation-staging.wtg.zone
    - hdr(host) -i triageassist-sand.wtg.zone
    - hdr(host) -i triageassist-api-sand.wtg.zone
    - hdr(host) -i ausyd-test1.transport.wisetechglobal.com
    - hdr(host) -i ausyd-test2.transport.wisetechglobal.com

ocp_grpc_domains:
  description: "OCP gRPC domains for internal au2prod1"
  tests:
    - hdr(host) -i wtg-ih-msgapi-1-messagesgateway.internal.au2prod1.ocp.wtg.ws
    - hdr(host) -i wtg-ih-msgapi-1-noauth-messagesgateway.internal.au2prod1.ocp.wtg.ws
    - hdr(host) -i wtg-ih-msgapi-1-noauth-messagesapi.internal.au2prod1.ocp.wtg.ws

star_internal_au2prod1_ocp_wtg_ws:
  description: "*.internal.au2prod1.ocp.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?internal\.au2prod1\.ocp\.wtg\.ws$

star_internal_au2prod2_ocp_wtg_ws:
  description: "*.internal.au2prod2.ocp.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?internal\.au2prod2\.ocp\.wtg\.ws$

platform-docs_wtg_ws:
  description: "Platform Docs"
  tests:
    - hdr(host),field(1,:) -i platform-docs.wtg.ws

star_wtg_zone:
  description: "*.wtg.zone"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?wtg\.zone$

star_wtg_ws:
  description: "*.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?wtg\.ws$

star_virt_wtg_ws:
  description: "*.virt.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?virt\.wtg\.ws$

borderwise_api_prod_url:
  description: "API Production Borderwise URL pattern"
  tests:
    - hdr(host) -m reg -i ^api\-((prod|prod-au2)\.)borderwise\.com$

star_app-uat_borderwise_com:
  description: "*.app-uat.borderwise.com"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?app-uat\.borderwise\.com$

redirects_to_ump_test1_borderwise_com_urls:
  description: "Redirect to ump-test1.borderwise.com"
  tests:
    - hdr(host) -m reg -i ^ump-test\.borderwise\.com$

redirects_to_umpapi_test1_borderwise_com_urls:
  description: "Redirect to umpapi-test1.borderwise.com"
  tests:
    - hdr(host) -m reg -i ^umpapi-test\.borderwise\.com$

star_connector_wisegrid_net:
  description: "*.connector.wisegrid.net"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?connector\.wisegrid\.net$

star_wisecloud_zone:
  description: "*.wisecloud.zone"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?wisecloud\.zone$

star_wisetechglobal_com:
  description: "*.wisetechglobal.com"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?wisetechglobal\.com$

star_sand_wtg_zone:
  description: "*.sand.wtg.zone"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?sand\.wtg\.zone$

au2s3-2_wtg_ws_and_subdomains:
  description: "*.au2s3-2.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^(.*)?au2s3-2\.wtg\.ws$

us2s3-2_wtg_ws_and_subdomains:
  description: "*.us2s3-2.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^(.*)?us2s3-2\.wtg\.ws$

de1s3-2_wtg_ws_and_subdomains:
  description: "*.de1s3-2.wtg.ws"
  tests:
    - hdr(host) -m reg -i ^(.*)?de1s3-2\.wtg\.ws$

au2s3-2-primary_not_enough_capacity:
  description: "au2s3-2 Primary backends unavailable"
  tests:
    - nbsrv(au2s3-2-primary) lt 1

de1s3-2-primary_not_enough_capacity:
  description: "de1s3-2 Primary backends unavailable"
  tests:
    - nbsrv(de1s3-2-primary) lt 1

us2s3-2-primary_not_enough_capacity:
  description: "us2s3-2 Primary backends unavailable"
  tests:
    - nbsrv(us2s3-2-primary) lt 1

star_transport_wisetechglobal_com:
  description: "*.transport.wisetechglobal.com"
  tests:
    - hdr(host),field(1,:) -m reg -i ^([^\.]+\.)?transport\.wisetechglobal\.com$

star_k8s_wtg_zone:
  description: "*.k8s.wtg.zone"
  tests:
    - hdr(host),field(1,:) -m reg -i ^([^\.]+\.)?k8s\.wtg\.zone$
