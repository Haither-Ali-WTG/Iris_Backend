---
star_wisetechglobal_com:
  description: "*.wisetechglobal.com"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?wisetechglobal\.com$

star_wtg_zone:
  description: "*.wtg.zone"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?wtg\.zone$

star_bolero_sand_wtg_zone:
  description: "*.bolero.sand.wtg.zone"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?bolero\.sand\.wtg\.zone$

star_testrig_sand_wtg_zone:
  description: |-
    *.testrig.sand.wtg.zone
    *.testrig3.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?testrig3?\.sand\.wtg\.zone$

star_webprint_testrig_sand_wtg_zone:
  description: |-
    *.webprint.testrig.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?webprint\testrig\.sand\.wtg\.zone$

star_localtransport_testrig_sand_wtg_zone:
  description: |-
    *.localtransport.testrig.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?localtransport\testrig\.sand\.wtg\.zone$

star_edirf_testrig_sand_wtg_zone:
  description: |-
    *.edirf.testrig.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?edirf\testrig\.sand\.wtg\.zone$

star_webcampaign_testrig_sand_wtg_zone:
  description: |-
    *.webcampaign.testrig.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?webcampaign\testrig\.sand\.wtg\.zone$

star_webtracker_testrig_sand_wtg_zone:
  description: |-
    *.webtracker.testrig.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?webtracker\testrig\.sand\.wtg\.zone$

star_cfs_testrig_sand_wtg_zone:
  description: |-
    *.cfs.testrig.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?cfs\testrig\.sand\.wtg\.zone$

star_scim_testrig_sand_wtg_zone:
  description: |-
    *.scim.testrig.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?scim\testrig\.sand\.wtg\.zone$

excluded_from_redirect_paths:
  description: |-
    Paths that should be excluded from the HTTPS redirect because
    the webservices don't support HTTPS
  tests:
    - path_beg -i /Warehouse/webservice

cw1_dynamic_backend_webservice_path:
  description: |-
    CW1 webservice paths that get routed to specific dynamic backends
  tests:
    - path_beg -i /Services/
    - path_beg -i /Glow/
    - path_beg -i /Portals/

testrig_healthcheck:
  description: |-
    Route healthchecks for testrig to default backend so even if a service goes down
    the healthcheck can still be reached for troubleshooting
  tests:
    - path_end -i /wtg/ready
    - path_end -i /wtg/status

star_sand_wtg_zone:
  description: "*.sand.wtg.zone"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?sand\.wtg\.zone$
    - hdr(host),field(1,:),lower -m reg -i ^([^\.]+\.)?temp\.sand\.wtg\.zone$

talogy_src:
  description: source IP is Talogy (3rd party vendor external) CS01854184
  tests:
    - src 40.118.100.127/32
    - var(txn.client_ip) -m ip 40.118.100.127/32
    - src 13.81.68.134/32
    - var(txn.client_ip) -m ip 13.81.68.134/32
    - src 13.81.68.146/32
    - var(txn.client_ip) -m ip 13.81.68.146/32
    - src 13.81.68.81/32
    - var(txn.client_ip) -m ip 13.81.68.81/32
    - src 13.81.64.194/32
    - var(txn.client_ip) -m ip 13.81.64.194/32
    - src 13.81.64.243/32
    - var(txn.client_ip) -m ip 13.81.64.243/32
    - src 13.81.65.246/32
    - var(txn.client_ip) -m ip 13.81.65.246/32

bsm_blume_src:
  description: source IP is BSM/Blume, requested by Ryken Song in CS01954112 for OCMBE (ocean carrier messaging)
  tests:
    - src 34.105.17.140/32
    - var(txn.client_ip) -m ip 34.105.17.140/32
    - src 34.82.183.200/32
    - var(txn.client_ip) -m ip 34.82.183.200/32
    - src 34.168.249.100/32
    - var(txn.client_ip) -m ip 34.168.249.100/32
    - src 34.105.0.45/32
    - var(txn.client_ip) -m ip 34.105.0.45/32

impexdocs_uat_src:
  description: ImpexDocs UAT service - requested by Gajendra Kumar in CS02188253 for OCMBE (ocean carrier messaging)
  tests:
    - src 103.127.43.222/32
    - var(txn.client_ip) -m ip 103.127.43.222/32

star_cargowise_com:
  description: "*.cargowise.com"
  tests:
    - hdr(host),field(1,:),lower -m reg -i ^([^\.]+\.)?cargowise\.com$

ocmbe_sand_wtg_zone:
  description: "ocmbe.sand.wtg.zone"
  tests:
    - hdr(host),field(1,:) -i ocmbe.sand.wtg.zone

star_wisegrid_net:
  description: "*.wisegrid.net"
  tests:
    - hdr(host),field(1,:) -m reg -i ^([^\.]+\.)?wisegrid\.net$

star_wtatestrig_sand_wtg_zone:
  description: |-
    *.wtatestrig.sand.wtg.zone
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?wtatestrig\.sand\.wtg\.zone$

custom_sandinternal_au2prod1_ocp_wtg_ws:
  description: "Custom sand internal au2prod1 OCP WTG WS hosts"
  tests:
    - hdr(host) -i terminator.sand.wtg.zone
    - hdr(host) -i cwbot.sand.wtg.zone
    - hdr(host) -i proget-staging-au-1.wisetechglobal.com
    - hdr(host) -i proget-staging-au-2.wisetechglobal.com
    - hdr(host) -i ocmportal.sand.wtg.zone
