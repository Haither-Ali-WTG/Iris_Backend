---
star_urs_wisegrid_net:
  description: "*.urs.wisegrid.net"
  tests:
    - hdr(host) -m reg -i ^([^\.]+\.)?urs\.wisegrid\.net$

query_urs_wisegrid_net:
  description: "query.urs.wisegrid.net"
  tests:
    - hdr(host) -i query.urs.wisegrid.net
