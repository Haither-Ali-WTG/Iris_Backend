---
map(include('acl'), key=regex('^[a-z][a-z0-9\-_]+$', name='lowercase label with underscores'))
---
acl:
  description: str()
  tests: list(include('test'))
test: regex('^[ -~]+$', name='ACL test')
