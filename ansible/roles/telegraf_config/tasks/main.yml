---
- name: Ensure telegraf.d directory exists
  file:
    path: /etc/telegraf/telegraf.d/
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: u=rwx,g=rx,o=rx
  become: yes

- name: Copy Prometheus config to telegraf.d
  template:
    src: prometheus.conf.j2
    dest: /etc/telegraf/telegraf.d/prometheus.conf
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: u=rw,g=r,o=r

- name: Check if telegraf's main service unit file exists
  find:
    paths:
      - /usr/lib/systemd/system
      - /etc/systemd/system
    patterns: 'telegraf.service'
  register: telegraf_unit_file

- name: Reload systemd daemon and restart telegraf if it exists
  become: yes
  systemd:
    name: telegraf
    state: restarted
  when:
    - telegraf_unit_file.matched > 0
