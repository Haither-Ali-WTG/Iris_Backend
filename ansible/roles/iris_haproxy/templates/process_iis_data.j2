clusters:
{% for cluster in cluster_names %}
  {{ cluster }}:
{%  for file in iis_files.files %}
{%    if cluster in (file.path | split('/')) %}
    {{ file.path | split('/') | last }}:
      {{ lookup('file', file.path) | from_yaml }}
{%    endif %}
{%  endfor %}
{% endfor %}
