---
- name: Start nodes
  include_role:
    name: kvm_start_nodes
  vars:
    add_drivers: no
    use_uefi: true
    host_bridge: oob-dante

- name: Wait 600 seconds for nodes to come up
  wait_for_connection:
    connect_timeout: 5
    timeout: 600
